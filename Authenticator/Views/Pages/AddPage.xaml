<Page
    x:Class="Authenticator_for_Windows.Views.Pages.AddPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Authenticator_for_Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Tag="Account toevoegen">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Padding="20" Grid.Row="0">
            <StackPanel Name="EntryBlockPanel" Padding="0 0 0 10" Visibility="Collapsed">

            </StackPanel>
            <TextBlock Text="Dienst:" />
            <TextBox Name="EntryService" HorizontalAlignment="Stretch" Margin="0 5 0 0" />
            <TextBlock Text="Gebruikersnaam:" Margin="0 10 0 5" />
            <TextBox Name="EntryUsername" HorizontalAlignment="Stretch" Margin="0 5 0 0" />
            <TextBlock Text="Code:" Margin="0 10 0 5" />
            <TextBox Name="EntryCode" HorizontalAlignment="Stretch" Margin="0 0 0 0" />
            <Button Name="Save" Content="Opslaan" Margin="0 5 0 20" HorizontalAlignment="Stretch" Click="Save_Click" />
        </StackPanel>
        <CommandBar Grid.Row="1" Name="CommandBar" VerticalAlignment="Bottom" Visibility="Collapsed">
            <AppBarButton Icon="Camera" Label="Scannen" Name="Scan" Click="Scan_Click" />
        </CommandBar>
    </Grid>
</Page>
