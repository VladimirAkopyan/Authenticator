<Page
    x:Class="Authenticator.AddPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Authenticator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.BottomAppBar>
        <CommandBar Name="CommandBar" Visibility="Collapsed">
            <AppBarButton Icon="Camera" Label="Scannen" Name="Scan" Click="Scan_Click" />
        </CommandBar>
    </Page.BottomAppBar>
    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="20">
        <TextBlock Text="Naam:" />
        <TextBox Name="EntryName" HorizontalAlignment="Stretch" Margin="0 5 0 0" />
        <TextBlock Text="Code:" Margin="0 10 0 5" />
        <TextBox Name="EntryCode" HorizontalAlignment="Stretch" Margin="0 0 0 0" />
        <Button Name="Save" Content="Opslaan" Margin="0 5 0 20" HorizontalAlignment="Stretch" Click="Save_Click" />
    </StackPanel>
</Page>
